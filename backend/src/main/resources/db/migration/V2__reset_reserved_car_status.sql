-- Доступность автомобилей теперь определяется датами броней, а не глобальным
-- статусом машины. Возвращаем в каталог авто, ранее заблокированные бронированием.
UPDATE cars SET status = 'AVAILABLE' WHERE status IN ('RESERVED', 'RENTED');
